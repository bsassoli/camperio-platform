# Configurazione camperio-platform (spec §7). Copia in:
#   Sviluppo: .env (spesso non serve: assenza di ORA_* = modalità DEMO)
#   VM:       /etc/camperio/camperio.env — root:docker, chmod 640, letto da compose
#             (`env_file:`), non da systemd: le unit non hanno EnvironmentFile=.
# I valori reali li inserisce Bernardino a mano al deploy — qui SOLO placeholder.

#ORA_USER=utenza_di_servizio_sola_lettura
#ORA_PWD=
#ORA_DSN=selmora01.ad.camperiosim.com:1521/<service_name>

#CAMPERIO_DATA=/var/lib/camperio

#ANTHROPIC_API_KEY=

# --- oauth2-proxy (deploy, ADR 0010) — valori dall'app registration Entra (richieste IT, punto B) ---
#OAUTH2_PROXY_PROVIDER=oidc
#OAUTH2_PROXY_OIDC_ISSUER_URL=https://login.microsoftonline.com/<TENANT_ID>/v2.0
#OAUTH2_PROXY_CLIENT_ID=
#OAUTH2_PROXY_CLIENT_SECRET=
#OAUTH2_PROXY_COOKIE_SECRET=   # openssl rand -base64 32 | tr '+/' '-_'  <- il tr e' obbligatorio: oauth2-proxy vuole base64 URL-safe
#OAUTH2_PROXY_REDIRECT_URL=https://app-ai.camperiosim.com/oauth2/callback
#OAUTH2_PROXY_ALLOWED_GROUPS=   # objectId del gruppo Entra abilitato — OBBLIGATORIO prima dell'esposizione
#OAUTH2_PROXY_WHITELIST_DOMAIN=app-ai.camperiosim.com   # per i redirect rd= assoluti
